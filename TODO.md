- don't use H-tags inside listing block, the block it self should be a list and the content should not use headline's.

## Styling

- improve the styles for the `imageGallery` listing variation (the image grid).
- improve the styles for the listing block's image list (the `summary` variation —
  list items with a preview image).

## Done

- `leadimage` block (Volto core "Lead Image Field") — rendered in Classic UI.
- `imageGallery` (Volto core listing variation) — rendered in Classic UI as an image grid.

