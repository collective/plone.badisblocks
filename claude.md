# important rules

- use plonecli skill where needed
- use this sveltekit reference implementation as inspiration: /home/maik/develop/svelteplayground/svolto/
- don't mention claude in commit messages and PR and descriptions and keep messages lean.
- plone is running on http://localhost:8080/Plone use admin:admin for the user, start restart it via "invoke debug"
- volto is running on http://localhost:3000 same user
- when done with a task, restart plone and use chrome dev mcp to verify that is worked
- don't look at bsbw blocks, check online volto-light-theme and volto core blocks, in version 19 or newer seven (aurora) implementations.
